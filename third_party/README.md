# Third-party code

`paper-search-mcp/` is vendored for a self-contained search backend. The application treats it as an executable backend through `bin/paper-search`; it is not imported as application code.
